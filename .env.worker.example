# ============================================================================
# RT-HERMES Worker Configuration
# ============================================================================
# Minimal configuration for distributed worker machines.
# Workers load database credentials from the Prefect Block.
#
# Prerequisites:
# 1. Host machine has run 'hermes db initialize' (which saves credentials to Prefect)
# 2. Worker can reach the Prefect server at PREFECT_API_URL
# 3. Worker can reach the PostgreSQL database (network access)
# ============================================================================

# Prefect server URL (required)
PREFECT_API_URL=http://host-machine:4200/api

# Load credentials from Prefect Block instead of local environment
CREDENTIAL_SOURCE=prefect
